# Base URL for the backend API.
# Leave empty or omitted in local development to use Vite's proxy (/api -> http://localhost:8080)
# In Vercel, set this to your deployed backend URL, e.g. https://your-backend.up.railway.app/api
VITE_API_BASE_URL=
